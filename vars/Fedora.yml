---

yum_keys: []

yum_repos: []

packages_to_install:
  - python-setuptools
  - python-pip
  - tmux
  - ruby
  - git
  - rsync

gem_executable: /opt/rh/rh-ruby23/root/usr/bin/gem

pip_executable: /usr/bin/pip
